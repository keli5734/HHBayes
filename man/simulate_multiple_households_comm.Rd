% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulator.R
\name{simulate_multiple_households_comm}
\alias{simulate_multiple_households_comm}
\title{Simulate Household Transmission}
\usage{
simulate_multiple_households_comm(
  n_households = 50,
  surveillance_df = NULL,
  start_date = "2024-07-01",
  end_date = "2025-06-30",
  alpha_comm_by_role = 5e-04,
  beta1 = 0.008,
  beta2 = 0.008,
  delta = 0,
  phi_by_role = c(adult = 1, infant = 4, toddler = 5, elderly = 1),
  kappa_by_role = c(adult = 1, infant = 1, toddler = 1.2, elderly = 1),
  infectious_shape = 3,
  infectious_scale = 1,
  waning_shape = 16,
  waning_scale = 10,
  peak_day = 1,
  width = 4,
  verbose = FALSE,
  seasonal_forcing_list = NULL,
  detect_threshold_log10 = 1e-06,
  detect_threshold_Ct = 99,
  surveillance_interval = 1,
  test_daily = FALSE,
  viral_testing = "viral load",
  V_ref = 3,
  V_rho = 2.5,
  Ct_50 = 40,
  Ct_delta = 2,
  VL_params_list = NULL,
  Ct_params_list = NULL,
  household_profile_list = NULL,
  perfect_detection = TRUE,
  contact_mat = NULL,
  role_mixing_matrix = NULL,
  model_type = "empirical",
  ODE_params_list = NULL,
  covariates_config = NULL,
  seed = NULL,
  max_infections = Inf
)
}
\arguments{
\item{n_households}{Integer. Number of households to simulate. Defaults to 50.}

\item{surveillance_df}{Dataframe with columns 'date' and 'cases' for community forcing. NULL for none.}

\item{start_date}{Character. Simulation start date ("YYYY-MM-DD"). Defaults to "2024-07-01".}

\item{end_date}{Character. Simulation end date ("YYYY-MM-DD"). Defaults to "2025-06-30".}

\item{alpha_comm_by_role}{Numeric or named vector. Community acquisition rate by role. Defaults to 5e-4.}

\item{beta1}{Numeric. Within-household transmission rate (pathway 1). Defaults to 8e-3.}

\item{beta2}{Numeric. Within-household transmission rate (pathway 2). Defaults to 8e-3.}

\item{delta}{Numeric. Co-infection parameter. Defaults to 0.}

\item{phi_by_role}{Named numeric vector. Susceptibility multipliers by role.}

\item{kappa_by_role}{Named numeric vector. Infectivity multipliers by role.}

\item{infectious_shape}{Numeric. Shape parameter for the Gamma infectious period. Defaults to 3.}

\item{infectious_scale}{Numeric. Scale parameter for the Gamma infectious period. Defaults to 1.}

\item{waning_shape}{Numeric. Shape parameter for the Gamma immunity waning period. Defaults to 16.}

\item{waning_scale}{Numeric. Scale parameter for the Gamma immunity waning period. Defaults to 10.}

\item{peak_day}{Numeric. Day of peak infectiousness. Defaults to 1.}

\item{width}{Numeric. Width of the infectiousness peak. Defaults to 4.}

\item{verbose}{Logical. Print progress messages. Defaults to FALSE.}

\item{seasonal_forcing_list}{List of numeric vectors for seasonal forcing by role. NULL for none.}

\item{detect_threshold_log10}{Numeric. Detection threshold on log10 viral load scale. Defaults to 1e-6.}

\item{detect_threshold_Ct}{Numeric. Detection threshold on Ct scale. Defaults to 99.}

\item{surveillance_interval}{Integer. Days between surveillance tests. Defaults to 1.}

\item{test_daily}{Logical. Whether to test daily. Defaults to FALSE.}

\item{viral_testing}{Character. Type of viral testing: "viral load" or "Ct". Defaults to "viral load".}

\item{V_ref}{Numeric. Reference viral load for transmission scaling. Defaults to 3.0.}

\item{V_rho}{Numeric. Exponent for viral load scaling. Defaults to 2.5.}

\item{Ct_50}{Numeric. Ct value at 50 percent detection probability. Defaults to 40.}

\item{Ct_delta}{Numeric. Steepness of Ct detection curve. Defaults to 2.}

\item{VL_params_list}{List of viral load trajectory parameters by role. NULL for defaults.}

\item{Ct_params_list}{List of Ct trajectory parameters by role. NULL for defaults.}

\item{household_profile_list}{List defining household composition probabilities. NULL for defaults.}

\item{perfect_detection}{Logical. Whether detection is perfect. Defaults to TRUE.}

\item{contact_mat}{Matrix. Custom contact matrix between individuals. NULL for default.}

\item{role_mixing_matrix}{Matrix. Contact weights between roles. 4x4 Matrix where element \link{i,j} represents
contact weight FROM role j TO role i. For asymmetric patterns,
role_mixing_matrix\link{adult, infant} ≠ role_mixing_matrix\link{infant, adult}.
Use dimnames(role_mixing_matrix) <- list(role_levels, role_levels). NULL for default.}

\item{model_type}{Character. Either "empirical" or "ODE". Defaults to "empirical".}

\item{ODE_params_list}{List of ODE parameters by role. NULL for defaults.}

\item{covariates_config}{List defining covariate configurations. NULL for none.}

\item{seed}{Integer. Random seed. NULL for no seed.}

\item{max_infections}{Numeric. Maximum infections per person. Defaults to Inf.}
}
\value{
A list with two elements: hh_df (household-level results) and diagnostic_df (testing results).
}
\description{
Simulates infection dynamics across multiple households with community forcing.
}
